services:
  pictiur:
    build: .
    image: pictiur:local
    container_name: pictiur
    restart: unless-stopped
    ports:
      - '3002:80'
    environment:
      # Auth basique optionnelle : laisser vide = accès libre
      - USERNAME=
      - PASSWORD=
